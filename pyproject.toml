[project]
name = "leetcode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rokkakudev", email = "dev_free_prog@yahoo.co.jp" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
leetcode = "leetcode:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
