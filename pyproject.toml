[project]
name = "leetcode"
version = "0.1.0"
description = "LeetCode practice for cloud/platform engineering interviews"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]
